<?php
use Drupal\ClassLearning\Common\Accordion,
  Drupal\ClassLearning\Models\WorkflowType as Workflow,
  Drupal\ClassLearning\Workflow\Generator;

/**
 * Workflow Management Views
 *
 * @package groupgrade
 * @subpackage workflows
 */

function groupgrade_workflow_index() {
  drupal_set_title(t('Workflow Management'));
  $return = '';

  return $return;
}


function groupgrade_create_workflow() {
  drupal_set_title(t('Create Workflow'));
  $return = '';

  return $return;
}